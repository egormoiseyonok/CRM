# Подробная инструкция: Локальный деплой с доступом из сети

## 🎯 Цель
Настроить ваш CRM на локальном компьютере так, чтобы другие пользователи в вашей сети могли к нему подключаться.

---

## 📋 Шаг 1: Определение IP-адреса

### Windows (PowerShell):
```powershell
ipconfig
```

Найдите строку **IPv4-адрес** в разделе вашего сетевого адаптера (Wi-Fi или Ethernet).

Пример: `192.168.1.100`

### Альтернативный способ:
1. Откройте командную строку (Win + R → `cmd`)
2. Введите: `ipconfig /all`
3. Найдите IPv4 Address

---

## 📋 Шаг 2: Настройка статического IP (РЕКОМЕНДУЕТСЯ)

Чтобы IP не менялся при переподключении:

1. **Откройте настройки сети:**
   - Win + I → Сеть и Интернет → Wi-Fi (или Ethernet)
   - Нажмите на ваше подключение → Свойства

2. **Найдите настройки IP:**
   - Прокрутите вниз до "Назначение IP"
   - Нажмите "Изменить"

3. **Установите вручную:**
   - Выберите "Вручную"
   - Введите ваш текущий IP (например, `192.168.1.100`)
   - Маска подсети: `255.255.255.0`
   - Шлюз: IP вашего роутера (обычно `192.168.1.1` или `192.168.0.1`)
   - DNS: `8.8.8.8` и `8.8.4.4` (Google DNS)

**Или настройте в роутере:**
- Зайдите в настройки роутера (обычно `192.168.1.1`)
- Найдите "DHCP Reservations" или "Static IP"
- Привяжите MAC-адрес вашего компьютера к IP

---

## 📋 Шаг 3: Настройка Apache в XAMPP

1. **Откройте файл конфигурации:**
   ```
   C:\xampp\apache\conf\httpd.conf
   ```

2. **Проверьте строку Listen:**
   Найдите и убедитесь, что есть:
   ```apache
   Listen 80
   ```

3. **Проверьте права доступа:**
   Найдите секцию:
   ```apache
   <Directory "C:/xampp/htdocs">
       Options Indexes FollowSymLinks
       AllowOverride All
       Require all granted
   </Directory>
   ```
   
   Убедитесь, что `Require all granted` присутствует (для Apache 2.4+)

4. **Сохраните и перезапустите Apache** через XAMPP Control Panel

---

## 📋 Шаг 4: Настройка файрвола Windows

### Способ 1: Через графический интерфейс

1. Откройте **Панель управления** → **Брандмауэр Защитника Windows**
2. Нажмите **Дополнительные параметры**
3. Выберите **Правила для входящих подключений**
4. Нажмите **Создать правило...**
5. Выберите **Для порта** → Далее
6. Выберите **TCP** и введите порт **80** → Далее
7. Выберите **Разрешить подключение** → Далее
8. Отметьте все профили (Домен, Частная, Публичная) → Далее
9. Назовите правило: "Apache HTTP Server" → Готово

### Способ 2: Через PowerShell (от имени администратора)

```powershell
New-NetFirewallRule -DisplayName "Apache HTTP Server" -Direction Inbound -Protocol TCP -LocalPort 80 -Action Allow
```

---

## 📋 Шаг 5: Настройка PostgreSQL для внешнего доступа

### 5.1. Настройка pg_hba.conf

1. Откройте файл:
   ```
   C:\xampp\pgsql\data\pg_hba.conf
   ```

2. Добавьте в конец файла (после существующих записей):
   ```
   # Разрешить доступ из локальной сети
   host    all    all    192.168.1.0/24    md5
   ```
   
   Где `192.168.1.0/24` - это ваша подсеть (замените на свою)

   **Или для доступа из любой подсети (менее безопасно):**
   ```
   host    all    all    0.0.0.0/0    md5
   ```

3. Сохраните файл

### 5.2. Настройка postgresql.conf

1. Откройте файл:
   ```
   C:\xampp\pgsql\data\postgresql.conf
   ```

2. Найдите строку:
   ```
   #listen_addresses = 'localhost'
   ```

3. Измените на:
   ```
   listen_addresses = '*'
   ```

4. Найдите строку с портом (обычно `port = 5432`) и убедитесь, что она не закомментирована

5. Сохраните файл

### 5.3. Перезапуск PostgreSQL

1. Откройте **XAMPP Control Panel**
2. Остановите PostgreSQL (Stop)
3. Запустите снова (Start)

### 5.4. Настройка файрвола для PostgreSQL

Создайте правило для порта 5432:

```powershell
New-NetFirewallRule -DisplayName "PostgreSQL" -Direction Inbound -Protocol TCP -LocalPort 5432 -Action Allow
```

---

## 📋 Шаг 6: Обновление config.php

Откройте `config.php` и измените:

```php
// Если доступ только из локальной сети - используйте IP вашего компьютера
define('DB_HOST', '192.168.1.100'); // Замените на ваш IP
define('APP_URL', 'http://192.168.1.100');
```

**ВАЖНО:** Если PostgreSQL работает на том же компьютере, что и веб-сервер, для локальных подключений можно оставить `localhost`, но для внешних подключений нужно использовать IP.

---

## 📋 Шаг 7: Тестирование

### 7.1. Локальное тестирование

1. Откройте браузер на том же компьютере
2. Перейдите: `http://localhost/CRM`
3. Убедитесь, что всё работает

### 7.2. Тестирование из сети

1. На другом устройстве в той же сети откройте браузер
2. Перейдите: `http://192.168.1.100/CRM` (замените на ваш IP)
3. Должен открыться сайт

### 7.3. Проверка подключения к PostgreSQL

На другом компьютере установите PostgreSQL клиент и попробуйте подключиться:

```bash
psql -h 192.168.1.100 -U postgres -d CRM
```

Или используйте pgAdmin или другой клиент.

---

## 🔧 Решение проблем

### Проблема: "Сайт недоступен" с другого устройства

**Решения:**
1. Проверьте, что Apache запущен
2. Проверьте файрвол Windows (порт 80 открыт)
3. Убедитесь, что устройства в одной сети
4. Попробуйте отключить файрвол временно для теста
5. Проверьте IP-адрес: `ipconfig` на сервере

### Проблема: "Ошибка подключения к БД" с другого устройства

**Решения:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте `pg_hba.conf` - правильная подсеть?
3. Проверьте `postgresql.conf` - `listen_addresses = '*'`?
4. Проверьте файрвол для порта 5432
5. Перезапустите PostgreSQL

### Проблема: IP-адрес меняется

**Решение:**
- Настройте статический IP (см. Шаг 2)
- Или используйте имя компьютера: `http://COMPUTER-NAME/CRM` (работает не всегда)

### Проблема: Медленное подключение

**Решения:**
1. Проверьте скорость Wi-Fi
2. Убедитесь, что устройства в одной сети
3. Проверьте нагрузку на сервер

---

## 🌐 Доступ из интернета (опционально)

Если нужно, чтобы доступ был не только из локальной сети, но и из интернета:

### Вариант 1: Проброс портов (Port Forwarding)

1. Зайдите в настройки роутера
2. Найдите "Port Forwarding" или "Виртуальные серверы"
3. Добавьте правило:
   - Внешний порт: 80
   - Внутренний IP: `192.168.1.100` (ваш IP)
   - Внутренний порт: 80
   - Протокол: TCP

4. Узнайте внешний IP: откройте [whatismyip.com](https://whatismyip.com)

5. Доступ: `http://ВАШ_ВНЕШНИЙ_IP/CRM`

**⚠️ ВАЖНО:** Это небезопасно! Используйте HTTPS и сильные пароли.

### Вариант 2: Динамический DNS (DuckDNS, No-IP)

1. Зарегистрируйтесь на [duckdns.org](https://www.duckdns.org) (бесплатно)
2. Создайте поддомен (например, `mycrm.duckdns.org`)
3. Установите обновлятель IP на компьютер
4. Настройте проброс портов в роутере
5. Доступ: `http://mycrm.duckdns.org/CRM`

---

## 🔒 Безопасность

### Рекомендации для локального деплоя:

1. **Используйте сильные пароли** для PostgreSQL и пользователей CRM
2. **Ограничьте доступ в pg_hba.conf** только нужными IP
3. **Не открывайте порты в интернет** без необходимости
4. **Регулярно обновляйте** XAMPP, PHP, PostgreSQL
5. **Делайте бэкапы** базы данных регулярно
6. **Используйте HTTPS** если открываете доступ в интернет (Let's Encrypt)

---

## 📝 Чеклист

- [ ] Определен IP-адрес компьютера
- [ ] Настроен статический IP (опционально)
- [ ] Apache настроен и запущен
- [ ] Файрвол Windows настроен для порта 80
- [ ] PostgreSQL настроен для внешнего доступа
- [ ] Файрвол Windows настроен для порта 5432
- [ ] config.php обновлен с правильным IP
- [ ] Протестирован доступ локально
- [ ] Протестирован доступ с другого устройства
- [ ] Настроены бэкапы (опционально)

---

## 💡 Дополнительные советы

1. **Используйте имя компьютера** вместо IP (если возможно):
   - `http://DESKTOP-ABC123/CRM`
   - Работает не всегда, зависит от настроек сети

2. **Создайте ярлык** на других компьютерах с URL вашего CRM

3. **Настройте автозапуск** Apache и PostgreSQL при загрузке Windows:
   - XAMPP Control Panel → Service → установите галочки

4. **Мониторинг:** Используйте XAMPP Control Panel для проверки статуса сервисов

5. **Бэкапы:** Настройте автоматический бэкап БД через планировщик задач Windows




