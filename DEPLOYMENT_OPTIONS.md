# Варианты деплоя CRM приложения

## 🏠 Локальный деплой с доступом из сети

### Вариант 1: Локальный ноутбук/ПК (БЕСПЛАТНО)

**Требования:**
- Ноутбук/ПК с Windows и XAMPP
- Все устройства в одной локальной сети (Wi-Fi/кабель)
- Настройка файрвола Windows

**Шаги:**

1. **Найти IP-адрес компьютера:**
   ```powershell
   ipconfig
   ```
   Найдите IPv4-адрес (например, `192.168.1.100`)

2. **Настроить Apache в XAMPP:**
   - Откройте `C:\xampp\apache\conf\httpd.conf`
   - Найдите строку `Listen 80` и убедитесь, что она есть
   - Найдите `<Directory "C:/xampp/htdocs">` и проверьте права доступа

3. **Настроить файрвол Windows:**
   - Панель управления → Брандмауэр Windows → Дополнительные параметры
   - Правила для входящих подключений → Создать правило
   - Тип: Для порта → TCP → Порт: 80
   - Разрешить подключение → Применить для всех профилей

4. **Настроить PostgreSQL для внешнего доступа:**
   - Откройте `C:\xampp\pgsql\data\pg_hba.conf`
   - Добавьте строку: `host    all    all    0.0.0.0/0    md5`
   - Откройте `C:\xampp\pgsql\data\postgresql.conf`
   - Найдите `listen_addresses` и установите: `listen_addresses = '*'`
   - Перезапустите PostgreSQL через XAMPP Control Panel

5. **Обновить config.php:**
   ```php
   define('DB_HOST', '192.168.1.100'); // IP вашего компьютера
   define('APP_URL', 'http://192.168.1.100');
   ```

6. **Доступ из сети:**
   - Другие пользователи открывают: `http://192.168.1.100/CRM`

**⚠️ ВАЖНО:** 
- Компьютер должен быть включен
- IP может измениться при переподключении к Wi-Fi (используйте статический IP в настройках роутера)

---

## ☁️ Бесплатные облачные платформы

### Вариант 2: Render.com (БЕСПЛАТНО с ограничениями)

**Плюсы:**
- Бесплатный тариф для веб-сервисов
- Автоматический деплой из Git
- Встроенная PostgreSQL база данных
- HTTPS из коробки

**Ограничения:**
- Приложение "засыпает" после 15 минут бездействия
- Первый запрос после сна занимает ~30 секунд
- 750 часов бесплатного времени в месяц

**Шаги:**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте PostgreSQL базу данных (Free tier)
3. Создайте Web Service:
   - Environment: PHP
   - Build Command: `composer install`
   - Start Command: `php -S 0.0.0.0:$PORT`
4. Добавьте переменные окружения:
   - `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASS`
5. Подключите GitHub репозиторий

**Стоимость:** БЕСПЛАТНО

---

### Вариант 3: Railway.app (БЕСПЛАТНО с кредитами)

**Плюсы:**
- $5 бесплатных кредитов в месяц
- Простой деплой
- PostgreSQL встроен

**Ограничения:**
- После исчерпания кредитов нужно платить (~$5-10/мес)

**Шаги:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект из GitHub
3. Добавьте PostgreSQL базу данных
4. Настройте переменные окружения
5. Railway автоматически определит PHP и задеплоит

**Стоимость:** БЕСПЛАТНО (первые $5), затем ~$5-10/мес

---

### Вариант 4: Heroku (ОГРАНИЧЕННО БЕСПЛАТНО)

**Плюсы:**
- Классическая платформа для деплоя
- PostgreSQL addon

**Ограничения:**
- Бесплатный тариф отменен в 2022 году
- Минимум $7/мес (Eco Dyno)

**Стоимость:** ~$7-25/мес

---

### Вариант 5: Fly.io (БЕСПЛАТНО с ограничениями)

**Плюсы:**
- 3 бесплатных VM
- PostgreSQL доступен
- Хорошая производительность

**Ограничения:**
- Ограниченные ресурсы на бесплатном тарифе

**Стоимость:** БЕСПЛАТНО (с ограничениями), затем ~$5-10/мес

---

## 💰 Дешевые VPS хостинги

### Вариант 6: DigitalOcean Droplet (~$4-6/мес)

**Плюсы:**
- Полный контроль
- Стабильная работа
- Хорошая документация

**Шаги:**
1. Создайте Droplet (Ubuntu 22.04)
2. Установите LAMP stack:
   ```bash
   sudo apt update
   sudo apt install apache2 postgresql php php-pgsql
   ```
3. Настройте Apache и PostgreSQL
4. Загрузите файлы через SFTP/Git

**Стоимость:** $4-6/мес

---

### Вариант 7: Vultr (~$2.50-6/мес)

**Плюсы:**
- Самый дешевый вариант
- Хорошая производительность
- Множество локаций

**Стоимость:** $2.50-6/мес

---

### Вариант 8: Hetzner Cloud (~€3-5/мес)

**Плюсы:**
- Немецкое качество
- Дешевле чем DigitalOcean
- Хорошая производительность

**Стоимость:** €3-5/мес (~$3-5)

---

## 🌐 Бесплатные PHP хостинги (с ограничениями)

### Вариант 9: InfinityFree / 000webhost

**Плюсы:**
- Полностью бесплатно
- Простая настройка

**Ограничения:**
- Нет PostgreSQL (только MySQL)
- Ограниченные ресурсы
- Реклама на бесплатном тарифе
- **Нужно будет переделать БД с PostgreSQL на MySQL**

**Стоимость:** БЕСПЛАТНО

---

## 🔧 Рекомендации по миграции для деплоя

### Если используете бесплатный хостинг с MySQL:

Вам нужно будет изменить `config.php`:

```php
// Вместо PostgreSQL
define('DB_HOST', 'localhost');
define('DB_PORT', '3306'); // MySQL порт
define('DB_NAME', 'your_database');
define('DB_USER', 'your_user');
define('DB_PASS', 'your_password');

// Изменить функцию getDB():
function getDB() {
    static $pdo = null;
    if ($pdo === null) {
        try {
            $dsn = "mysql:host=" . DB_HOST . ";port=" . DB_PORT . ";dbname=" . DB_NAME . ";charset=utf8mb4";
            $pdo = new PDO($dsn, DB_USER, DB_PASS);
            $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
            $pdo->setAttribute(PDO::ATTR_DEFAULT_FETCH_MODE, PDO::FETCH_ASSOC);
        } catch (PDOException $e) {
            die("Ошибка подключения к БД: " . $e->getMessage());
        }
    }
    return $pdo;
}
```

И изменить SQL синтаксис:
- `||` (конкатенация в PostgreSQL) → `CONCAT()` в MySQL
- `NULLS LAST` → убрать или использовать `ISNULL()`
- Другие различия в SQL

---

## 📊 Сравнительная таблица

| Вариант | Стоимость | Сложность | PostgreSQL | Доступ из интернета |
|---------|-----------|-----------|------------|---------------------|
| Локальный ПК | БЕСПЛАТНО | Средняя | ✅ Да | ❌ Только локальная сеть |
| Render.com | БЕСПЛАТНО | Низкая | ✅ Да | ✅ Да |
| Railway.app | $0-10/мес | Низкая | ✅ Да | ✅ Да |
| Fly.io | БЕСПЛАТНО | Средняя | ✅ Да | ✅ Да |
| DigitalOcean | $4-6/мес | Высокая | ✅ Да | ✅ Да |
| Vultr | $2.50-6/мес | Высокая | ✅ Да | ✅ Да |
| Hetzner | €3-5/мес | Высокая | ✅ Да | ✅ Да |
| InfinityFree | БЕСПЛАТНО | Низкая | ❌ Нет (MySQL) | ✅ Да |

---

## 🎯 Рекомендации

### Для начала (тестирование):
1. **Локальный ПК** - если нужен доступ только в офисе/дома
2. **Render.com** - если нужен доступ из интернета бесплатно

### Для продакшена:
1. **Railway.app** или **Fly.io** - если бюджет ограничен
2. **Vultr** или **Hetzner** - если нужен полный контроль и стабильность

### Для максимальной экономии:
1. **Локальный ПК** + настройка динамического DNS (DuckDNS, No-IP) для доступа из интернета

---

## 🔒 Безопасность

**ВАЖНО при деплое:**
1. Измените пароли БД на сложные
2. Не коммитьте `config.php` в Git (используйте `.env` или `config.example.php`)
3. Настройте HTTPS (Let's Encrypt бесплатно)
4. Ограничьте доступ к PostgreSQL только с нужных IP
5. Регулярно делайте бэкапы БД

---

## 📝 Чеклист для деплоя

- [ ] Создать `.gitignore` (исключить `config.php`)
- [ ] Настроить переменные окружения
- [ ] Создать дамп базы данных
- [ ] Настроить права доступа к файлам
- [ ] Настроить HTTPS
- [ ] Настроить автоматические бэкапы
- [ ] Протестировать доступ извне
- [ ] Настроить мониторинг (опционально)



